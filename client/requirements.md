## Packages
framer-motion | Complex page transitions and scroll animations
clsx | Utility for constructing className strings conditionally
tailwind-merge | Utility for merging Tailwind CSS classes
lucide-react | Beautiful icons for UI elements

## Notes
Tailwind Config - extend fontFamily:
fontFamily: {
  sans: ["'Tajawal'", "sans-serif"],
  display: ["'Aref Ruqaa'", "serif"],
}
RTL direction will be enforced via the `dir="rtl"` attribute on the root element.
Background hero images are located at `/images/hero1.png` and `/images/hero2.png`.
